#pragma once
/* Callback types are defined in esp_ble_mesh_defs.h shim.
   The ESP provisioning API calls are inside #ifndef HOST_SIM blocks
   in gradient_mesh.c and are never compiled on the host. */
